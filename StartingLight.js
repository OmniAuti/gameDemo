export default class StartingLight {

    constructor(ctx, height, speed, scaleRatio, startingCount) {
        this.ctx = ctx;
        this.canvas = ctx.canvas;
        this.height = height;
        this.speed = speed;
        this.scaleRatio = scaleRatio;
        this.startingCount = startingCount;
    }


    draw() {

    }

    update() {
        
    }

    reset() {

    }
}